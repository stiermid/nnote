nnote edit
==========

.. code-block:: text

   nnote edit <title> [-d <dir>]

Open an existing note in the configured editor. Errors if the note doesn't
exist (use ``new`` to create it).

.. code-block:: bash

   nnote edit todo
