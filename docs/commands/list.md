# nnote list

```
nnote list [-d <dir>]
```

Display all notes as a tree. Scope to a subdirectory with `-d`.

```bash
nnote list
nnote list -d work
```

```
/home/user/nnotes
├── work/
│   ├── meeting
│   └── standup
└── todo
```
